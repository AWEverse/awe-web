import { FC, ReactNode } from "react";

interface OwnProps {
  children: ReactNode;
}

interface StateProps {}

const CommunityFooter: FC<OwnProps & StateProps> = () => {
  return <></>;
};

export default CommunityFooter;
