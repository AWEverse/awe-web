import { FC, ReactNode } from 'react';

interface OwnProps {
  children: ReactNode;
}

interface StateProps {}

const ChapterChain: FC<OwnProps & StateProps> = () => {
  return <></>;
};

export default ChapterChain;
